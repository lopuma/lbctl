# TODO OS
COVER_DIR: la ubicación del directorio donde se almacenan las portadas de libros.
MY_USER: Usuario con el que se construira el contenedor del proyecto lbctl

# TODO SELENIUM
WEBDRIVER_HOST: la dirección IP del host donde se ejecuta el servidor Selenium.
WEBDRIVER_STANDALONE: el navegador web que se utilizará para la automatización de pruebas. Las opciones son "firefox", "chrome" o "edge".
WEBDRIVER_PORT_CLI: el puerto en el que se ejecutará el servidor Selenium para la comunicación cliente-servidor.
WEBDRIVER_PORT_WEB: el puerto en el que se ejecutará el servidor Selenium para la interacción con el navegador web.

# TODO MYSQL
MYSQL_HOST: la dirección IP del host donde se ejecuta el servidor de base de datos MySQL.
MYSQL_USER: el nombre de usuario que se utilizará para conectarse al servidor de base de datos.
MYSQL_PASSWORD: la contraseña que se utilizará para conectarse al servidor de base de datos.
MYSQL_DATABASE: el nombre de la base de datos que se utilizará para la aplicación.
MYSQL_PORT: el puerto en el que se ejecutará el servidor de base de datos MySQL.

# TODO ENV REDIS
REDIS_HOST: la dirección IP del host donde se ejecuta el servidor Redis.
REDIS_PORT: el puerto en el que se ejecutará el servidor Redis.
REDIS_PASSWORD: la contraseña que se utilizará para conectarse al servidor Redis.
REDIS_COMMANDER_PORT: el puerto en el que se ejecutará el servidor web para Redis Commander.

# TODO ENV BUCKET
BUCKET_NAME: el nombre del bucket que se utilizará para almacenar los objetos en MinIO.
USE_SSL: indica si se utilizará SSL o no. Si está establecido en true, se utilizará SSL. Si está establecido en false o se omite, no se utilizará SSL.
MINIO_HOST: la dirección IP del host donde se ejecuta el servidor MinIO.
MINIO_PORT: el puerto en el que se ejecutará el servidor MinIO.
MINIO_CONSOLE_PORT: el puerto en el que se ejecutará la consola de administración de MinIO.
MINIO_USER: el nombre de usuario que se utilizará para autenticar al servidor MinIO.
MINIO_PASSWORD: la contraseña que se utilizará para autenticar al servidor MinIO.